function w = get_load_profile(w,h,nr_areas)
    w_hour = load('data\w_load.mat');
    
    for k=1:size(w,2)
        t = k*h;
        if(t < hour)
            w()
            
        end
    end
    w = w;
end

